name: ilkersevim_json_isolate
description: >-
  JSON map/list decode and encode helpers that offload large payloads with
  Flutter compute.
version: 0.1.1
repository: https://github.com/redjadet/ilkersevim_json_isolate
homepage: https://github.com/redjadet/ilkersevim_json_isolate
issue_tracker: https://github.com/redjadet/ilkersevim_json_isolate/issues
topics:
  - json
  - isolate
  - flutter
  - compute

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
